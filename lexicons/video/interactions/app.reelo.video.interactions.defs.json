{
    "lexicon": 1,
    "id": "app.reelo.video.interactions.defs",
    "defs": {
      "moderationStatus": {
        "type": "string",
        "description": "Represents moderation status for an interaction",
        "enum": ["active", "removed", "silenced", "adult"]
      },
      "subjectReference": {
        "type": "object",
        "description": "A reference to the subject of an interaction (e.g. a video or a comment)",
        "required": ["uri"],
        "properties": {
          "uri": {
            "type": "string",
            "description": "URI pointing to a record, such as a video post or comment"
          },
          "cid": {
            "type": "string",
            "description": "An optional content identifier for versioning"
          }
        }
      }
    }
  }
  